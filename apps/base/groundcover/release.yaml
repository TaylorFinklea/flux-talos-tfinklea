apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: groundcover
  namespace: groundcover
spec:
  releaseName: groundcover
  chart:
    spec:
      chart: groundcover
      sourceRef:
        kind: HelmRepository
        name: groundcover
  interval: 50m
  install:
    remediation:
      retries: 3
  # values:
  #   ui:
  #     color: '#18CAE6'
